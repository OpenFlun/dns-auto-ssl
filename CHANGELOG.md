# 变更日志
## [1.03] - 2026-06-12 16:35
### 优化
- 内部一些优化
## [1.02] - 2026-05-30 11:30
### 优化
- **优化续期任务检测逻辑**：从检查脚本文件是否存在改为基于时间戳标志文件（`.${mainDomain}.renew-stamp`），准确判断续期任务是否有效（有效期为 24 小时）。
- **修复重复安装漏洞**：安装完成后立即执行一次续期脚本，自动生成初始标志文件，避免同一天内多次调用 `setup` 时重复安装。
- **增强验证机制**：安装续期任务后立即运行一次脚本，验证环境变量和脚本可执行性，失败则报错退出。
- **代码重构**：将 `getRenewStampPath` 提取到 `shard.js` 中统一管理，消除多处重复定义。
- **保留原有兼容性**：Linux/macOS 使用 crontab，Windows 使用计划任务，续期脚本每次执行均刷新标志文件（续期时间滚动延续）。